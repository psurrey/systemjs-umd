console.log('In index.js');

// React works...
var React = require('react');
console.log(React);

/*

Uncomment this, run "npm start" again and you will see that the app does not load... => No "In index.js" in console

var ReactDataGrid = require('react-data-grid');
console.log(ReactDataGrid);
 */
